import { Flex } from "@chakra-ui/react";


const Header = () => {
    return (
        <Flex alignItems="center" justifyContent="center" fontSize={40} flex={1} >
            Welcome to the Dummy Store
        </Flex>
    );
};

export default Header;